#!/bin/bash
set -eo pipefail

find_matches_of_type() {
	pushd "$HOME" &> /dev/null
	find . -maxdepth 1 -type "$1" -name '.[^(DS_Store|Trash|dotfiles)]*'
	popd &> /dev/null
}

find_matches_of_type f
find_matches_of_type d